Prompt #2 - Refatorar PerformanceModal
Agora vamos refatorar a PerformanceModal.tsx (810 linhas) e adicionar o grid de detalhes que está faltando.
Copie e cole no Replit:
# TAREFA: Refatorar PerformanceModal + Adicionar Grid de Detalhes## ⚠️ REGRAS IMPORTANTES- NÃO alterar estética/visual existente (cores, animações, fontes)- Manter TODAS as funcionalidades das 4 abas (Market Share, Business, Operational, Expenses)- O modal deve continuar funcionando perfeitamente após as alterações---## PROBLEMA 1: Arquivo muito grande (810 linhas)O arquivo `client/src/components/dashboard/modals/PerformanceModal.tsx` precisa ser refatorado.### AÇÃO: Extrair gráficos para arquivos separadosCriar pasta e arquivos:
client/src/components/dashboard/modals/performance/
├── index.ts # Re-export do PerformanceModal
├── MarketShareChart.tsx # Componente do gráfico Market Share
├── BusinessLineChart.tsx # Componente do gráfico Business
├── OperationalBarChart.tsx # Componente do gráfico Operational
├── KPICard.tsx # Componente de card KPI
└── PremiumTabs.tsx # Componente das abas
O `PerformanceModal.tsx` deve importar esses componentes e ficar com no máximo ~300 linhas.---## PROBLEMA 2: Falta o Grid de Detalhes na parte inferiorNa versão original do xFinance (Dash), o modal de Performance tinha um **grid de detalhes** na parte inferior que mostrava os dados tabulados.### AÇÃO: Adicionar seção "Detalhes" com gridAdicionar ABAIXO do conteúdo das abas (após o Card com os gráficos):{/* Grid de Detalhes */}<motion.div variants={itemVariants} className="mt-6">  <Card className="glass border-white/10 bg-[rgba(15,15,35,0.6)]">    <CardContent className="p-4">      <div className="flex items-center justify-between mb-4">        <div className="flex items-center gap-2">          <FileText className="w-4 h-4 text-primary" />          <h3 className="text-sm font-semibold text-foreground">Detalhes</h3>        </div>        <Badge variant="outline" className="text-xs border-white/20">          {/* Quantidade de registros */}          0 registros        </Badge>      </div>            <div className="rounded-lg border border-white/10 overflow-hidden">        <Table>          <TableHeader>            <TableRow className="border-b border-white/10 bg-white/5">              <TableHead className="text-xs font-semibold text-muted-foreground">Player</TableHead>              <TableHead className="text-xs font-semibold text-muted-foreground">Segurado</TableHead>              <TableHead className="text-xs font-semibold text-muted-foreground">Guy</TableHead>              <TableHead className="text-xs font-semibold text-muted-foreground text-right">Honorários</TableHead>              <TableHead className="text-xs font-semibold text-muted-foreground text-right">Despesas</TableHead>              <TableHead className="text-xs font-semibold text-muted-foreground text-center">Envio</TableHead>              <TableHead className="text-xs font-semibold text-muted-foreground text-center">Pago</TableHead>            </TableRow>          </TableHeader>          <TableBody>            {/* Dados mockados por enquanto */}            {[              { player: "AON", segurado: "PETROBRAS", guy: "Marcus", honorarios: 15000, despesas: 1200, envio: "15/12", pago: "20/12" },              { player: "MARSH", segurado: "VALE", guy: "Alexander", honorarios: 12500, despesas: 800, envio: "10/12", pago: "18/12" },              { player: "AON", segurado: "BRASKEM", guy: "André", honorarios: 8900, despesas: 650, envio: "12/12", pago: "-" },            ].map((row, idx) => (              <TableRow key={idx} className="border-b border-white/5 hover:bg-white/[0.02]">                <TableCell className="text-xs font-medium">{row.player}</TableCell>                <TableCell className="text-xs text-muted-foreground">{row.segurado}</TableCell>                <TableCell className="text-xs text-muted-foreground">{row.guy}</TableCell>                <TableCell className="text-xs text-right font-mono text-success">                  {new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL", minimumFractionDigits: 0 }).format(row.honorarios)}                </TableCell>                <TableCell className="text-xs text-right font-mono text-destructive">                  {new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL", minimumFractionDigits: 0 }).format(row.despesas)}                </TableCell>                <TableCell className="text-xs text-center text-muted-foreground">{row.envio}</TableCell>                <TableCell className="text-xs text-center text-muted-foreground">{row.pago}</TableCell>              </TableRow>            ))}          </TableBody>        </Table>      </div>            {/* Paginação simples */}      <div className="flex items-center justify-between mt-3 text-xs text-muted-foreground">        <span>Mostrando 1-3 de 3</span>        <div className="flex items-center gap-1">          <Button variant="ghost" size="icon" className="h-7 w-7" disabled>            <ChevronLeft className="w-3.5 h-3.5" />          </Button>          <Button variant="ghost" size="icon" className="h-7 w-7" disabled>            <ChevronRight className="w-3.5 h-3.5" />          </Button>        </div>      </div>    </CardContent>  </Card></motion.div>---## ESTRUTURA FINAL ESPERADA
ESTRUTURA FINAL ESPERADA
modals/├── PerformanceModal.tsx (~300 linhas) - Importa componentes da pasta performance/└── performance/    ├── index.ts    ├── MarketShareChart.tsx (~100 linhas)    ├── BusinessLineChart.tsx (~200 linhas)    ├── OperationalBarChart.tsx (~100 linhas)    ├── KPICard.tsx (~50 linhas)    └── PremiumTabs.tsx (~50 linhas)over funcionalidades das abas- NÃO alterar os dados mockados dos gráficos- NÃO mudar o nome do componente exportado
IMPORTS NECESSÁRIOS NO PerformanceModal
Adicionar imports para o grid:
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";import { ChevronLeft, ChevronRight } from "lucide-react";
NÃO FAZER
NÃO alterar cores, animações ou espaçamentos existentes
NÃO remover funcionalidades das abas
NÃO alterar os dados mockados dos gráficos
NÃO mudar o nome do componente exportado