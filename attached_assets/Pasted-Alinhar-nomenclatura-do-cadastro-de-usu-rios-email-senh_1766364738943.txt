Alinhar nomenclatura do cadastro de usuários (email/senha/papel)

- Leia e aplique este contexto antes de qualquer implementação:
  
  - Projeto: xFinance 3.0 — migração do frontend para React/TypeScript + FastAPI.
  - Banco: SQLite ( xFinanceDB.db ) original; manter nomenclaturas alinhadas para reaproveitar queries.
  - Regras: não alterar estética/tema (glassmorphism, lilás/ciano, gradientes), não mudar estrutura de componentes, não mexer nas animações Framer Motion, não adicionar/remover dependências, usar Radix UI conforme já adotado.
- Objetivo:
  
  - Alinhar a tela e serviços de “Usuários” para usar as chaves externas email , senha , papel , ativo (compatível com o sistema original), mantendo o mapeamento interno para user.email , user.hash_senha , user.papel , user.ativo no backend.
  - Não mudar a UI, apenas os tipos/DTOs, validações e chamadas de API.
- Entregáveis:
  
  - Criar client/src/types/usuario.ts com:
    - type Papel = 'admin' | 'financeiro' | 'analista' | 'auditor'
    - interface UsuarioInput { email: string; senha: string; papel: Papel; ativo: boolean; nome?: string; nick?: string; short_nome?: string }
    - interface Usuario { id_user: number; email: string; papel: Papel; ativo: boolean; nome?: string; nick?: string; short_nome?: string }
  - Criar client/src/services/api/usuarios.ts :
    - Base: const BASE_URL = import.meta.env.VITE_API_URL || "/api"
    - async function createUsuario(data: UsuarioInput): Promise<Usuario> :
      - POST ${BASE_URL}/usuarios com Content-Type: application/json
      - Nunca logar senha
      - Tratar 4xx/5xx com mensagens amigáveis
    - async function fetchUsuarios(): Promise<Usuario[]> (stub opcional)
  - Atualizar client/src/components/dashboard/modals/UsersModal.tsx para:
    - Usar UsuarioInput ( email , senha , papel , ativo ) no estado e submit
    - Validar:
      - email : formato válido
      - senha : mínimo 8 caracteres com número e letra; campo “confirmar senha” igual
      - papel : obrigatório entre os valores de Papel
      - ativo : booleano
    - Em sucesso: fechar modal, exibir toast “Usuário criado com sucesso”, limpar campos senha e confirmarSenha
  - Ajustar qualquer uso antigo de username / password para email / senha :
    - Não alterar shared/schema.ts (drizzle) — manter mapeamento interno ( email , hash_senha , papel , ativo ) apenas como detalhe de persistência
    - Garantir que o frontend nunca envia hash_senha ; envia senha e o backend faz o hash
- Regras de compatibilidade:
  
  - papel deve usar exatamente o domínio: admin , financeiro , analista , auditor
  - Stubs locais podem retornar Usuario com id_user fake enquanto o FastAPI não estiver disponível
  - Não alterar estilos, classes CSS, nem estrutura de componentes
- Aceitação:
  
  - A modal de usuários compila sem novas dependências
  - Payload do POST /usuarios usa as chaves email , senha , papel , ativo
  - Validações funcionam e desabilitam “Salvar” quando houver erros
  - Toast de sucesso e limpeza de senha acontecem corretamente
  - Nenhum log de senha é emitido em console ou rede